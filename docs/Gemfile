source "https://rubygems.org"

# This is the version GitHub Pages itself builds with -
# keeping it pinned means "looks right locally" == "looks right on github.com"
gem "github-pages", group: :jekyll_plugins
